# GFX.WebView examples

- `Availability.sx` inspects the selected platform adapter without creating a
  window.
- `BridgeWebPage.sx` exchanges named messages with embedded content.
- `EmbeddedFiles.sx` embeds HTML, CSS and JavaScript in the executable.
