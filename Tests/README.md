# GFX.WebView tests

The top-level `.sx` files validate the package implementation directly.
`Consumer` is an anonymous application workspace executed from its own
directory to verify the public package and catalog boundaries.
